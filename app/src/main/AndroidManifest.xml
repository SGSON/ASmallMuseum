<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="sg.asmallmuseum">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ASmallMuseum">
        <activity android:name=".presentation.EmailVerifiedActivity"></activity>
        <activity android:name=".presentation.ConfirmUpdataActivity" />
        <activity android:name=".presentation.UpdateUserInfoActivity" />
        <activity android:name=".presentation.UpdatePasswordActivity" />
        <activity android:name=".presentation.UserProfileActivity" />
        <activity android:name=".presentation.SignInWelcomeActivity" />
        <activity android:name=".presentation.FacebookSignInActivity" />
        <activity android:name=".presentation.GoogleSignInActivity" />
        <activity android:name=".presentation.GoogleUserDBActivity" />
        <activity android:name=".presentation.FacebookUserDBActivity" />
        <activity android:name=".presentation.ArtViewActivity" />

        <meta-data
            android:name="com.facebook.sdk.AutoLogAppEventsEnabled"
            android:value="false" />
        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />

        <activity
            android:name="com.facebook.FacebookActivity"
            android:configChanges="keyboard|keyboardHidden|screenLayout|screenSize|orientation"
            android:label="@string/app_name" />
        <activity
            android:name="com.facebook.CustomTabActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="@string/fb_login_protocol_scheme" />
            </intent-filter>
        </activity>
        <activity android:name=".presentation.SignUp" />
        <activity android:name=".presentation.Login" />
        <activity android:name=".presentation.LoginForm" />
        <activity android:name=".presentation.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>